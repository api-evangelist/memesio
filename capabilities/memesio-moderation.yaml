naftiko: 1.0.0-alpha2
info:
  label: Memesio API Contracts — moderation
  description: 'Memesio API Contracts — moderation. 1 operations. Lead operation: Moderate Caption Candidates with Inline
    Highlights/Rationale and Enqueue Blocked Outputs. Self-contained Naftiko capability covering one Memesio business surface.'
  tags:
  - Memesio
  - moderation
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    MEMESIO_API_KEY: MEMESIO_API_KEY
capability:
  consumes:
  - type: http
    namespace: memesio-moderation
    baseUri: ''
    description: Memesio API Contracts — moderation business capability. Self-contained, no shared references.
    resources:
    - name: api-ai-captions-moderate
      path: /api/ai/captions/moderate
      operations:
      - name: post
        method: POST
        description: Moderate Caption Candidates with Inline Highlights/Rationale and Enqueue Blocked Outputs
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: apikey
      key: x-developer-api-key
      value: '{{env.MEMESIO_API_KEY}}'
      placement: header
  exposes:
  - type: rest
    namespace: memesio-moderation-rest
    port: 8080
    description: REST adapter for Memesio API Contracts — moderation. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/api/ai/captions/moderate
      name: api-ai-captions-moderate
      description: REST surface for api-ai-captions-moderate.
      operations:
      - method: POST
        name: post
        description: Moderate Caption Candidates with Inline Highlights/Rationale and Enqueue Blocked Outputs
        call: memesio-moderation.post
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: memesio-moderation-mcp
    port: 9090
    transport: http
    description: MCP adapter for Memesio API Contracts — moderation. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: moderate-caption-candidates-inline-highlights
      description: Moderate Caption Candidates with Inline Highlights/Rationale and Enqueue Blocked Outputs
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: memesio-moderation.post
      outputParameters:
      - type: object
        mapping: $.
